进程4要素：
1、进程表（包含LDT）
2、进程体
3、GDT(LDT描述符、TSS描述符)
4、TSS

添加一个任务、进程的步骤:
1、NR_TASK 或 NR_PROC加一						const.h
2、添加任务 或 进程体（同时要在proto.h中添加函数申明）			任何文件内都可以
3、任务、进程初始化信息表中添加任务入口、任务名，优先级，特权级等信息		task.c
4、(进程表、进程堆栈由程序自动添加，GDT中的LDT描述符由程序自动初始化)

覃攀 2013-11-22 13:31